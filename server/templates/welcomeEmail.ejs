<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= brandName %> - Verify Your Email
    </title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f7f7f7;
            color: #333333;
        }

        .container {
            max-width: 500px;
            margin: 0 auto;
            padding: 20px;
        }

        .email-wrapper {
            background-color: #ffffff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .logo {
            text-align: center;
            padding: 25px 0 15px;
        }

        .logo img {
            max-width: 200px;
            height: 50px;
        }

        .email-content {
            padding: 20px 30px;
        }

        .verification-box {
            background-color: #f2f5fd;
            border-radius: 8px;
            padding: 25px 20px;
            margin: 15px 0;
            text-align: center;
        }

        .verification-link {
            margin: 20px auto;
        }

        .btn {
            display: inline-block;
            background-color: #3f51b5;
            color: white;
            text-decoration: none;
            padding: 12px 24px;
            border-radius: 4px;
            font-size: 16px;
            font-weight: bold;
        }

        .otp-box {
            background-color: #ffffff;
            border: 1px dashed #3f51b5;
            padding: 12px;
            border-radius: 5px;
            font-size: 24px;
            letter-spacing: 6px;
            text-align: center;
            font-weight: bold;
            margin: 15px 0;
            color: #3f51b5;
        }

        .footer {
            padding: 15px 30px;
            text-align: center;
            font-size: 12px;
            color: #666666;
            border-top: 1px solid #eeeeee;
        }

        .divider {
            margin: 20px 0;
            text-align: center;
            font-size: 14px;
            color: #888;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="email-wrapper">
            <div class="logo">
                <!-- Replace brand name with actual logo image -->
                <img src="<%= process.env.BRAND_LOGO_IMAGE_URL %>" alt="<%= brandName %>" class="logo" />
            </div>

            <div class="email-content">
                <h3 style="margin-top: 0;">Welcome to <%= brandName %>
                </h3>
                <p>Please verify your email to complete your registration.</p>

                <div class="verification-box">
                    <div class="verification-link">
                        <a href="<%= verificationLink %>" class="btn" style="color: #ffffff;">Click here to verify your
                            email</a>
                    </div>

                    <div class="divider">or use this code</div>

                    <div class="otp-box">
                        <%= otp %>
                    </div>
                    <p style="font-size: 13px; margin: 0;">Code expires in 1 hour</p>
                </div>

                <p style="font-size: 14px; margin-bottom: 0;">Need help? <a href="<%= supportLink %>"
                        style="color: #3f51b5;">Contact our support team</a></p>
            </div>

            <div class="footer">
                <p>&copy; <%= year %>
                        <%= brandName %>. All rights reserved.</p>
                <p style="margin: 5px 0 0;">If you didn't create an account, please ignore this email.</p>
            </div>
        </div>
    </div>
</body>

</html>