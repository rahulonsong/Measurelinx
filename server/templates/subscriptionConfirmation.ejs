<!DOCTYPE html>
<html>

<head>
    <title>Subscription Confirmation</title>
    <style>
        /* Add your custom CSS styles here */
        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 16px;
            color: #333;
        }

        .container {
            margin: 20px auto;
            max-width: 600px;
            padding: 20px;
            background-color: #f4f4f4;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .logo {
            display: block;
            margin: 0 auto;
            max-width: 200px;
            height: 50px;
        }

        h1 {
            margin-top: 30px;
            margin-bottom: 20px;
            text-align: center;
            color: #444;
            font-size: 24px;
        }

        p {
            margin-bottom: 20px;
            line-height: 1.5;
        }

        .btn {
            display: inline-block;
            background-color: #abe1e1;
            color: rgb(5, 24, 25);
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 5px;
            margin-top: 20px;

        }


        .btn:hover {
            background-color: #83eac6;
            cursor: pointer;
        }

        .btn__position {
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .unsubscribe {
            margin-top: 50px;
            text-align: center;
            font-size: 14px;
            color: #999;
        }

        .unsubscribe a {
            color: #214264;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="container">
        <img class="logo" src="<%= process.env.BRAND_LOGO_IMAGE_URL %>" alt="Logo" />
        <h1>Thank You for Subscribing!</h1>
        <p>You have successfully subscribed to our newsletter. We will be sending you regular updates and offers.</p>
        <p>If you have any questions or concerns, please do not hesitate to contact us.</p>
        <div class="btn__position">
            <a href="<%= url %>" class="btn">Visit Our Website</a>
        </div>
        <div class="unsubscribe">
            <p>If you wish to unsubscribe from our newsletter, please click <a
                    href="<%= url +'/unsubscribe/' + email %>">here</a>.</p>
        </div>
    </div>
</body>

</html>